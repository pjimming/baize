syntax = "v1"

info(
	title: "type title here"
	desc: "type desc here"
	author: "type author here"
	email: "type email here"
	version: "type version here"
)

type (
	PongResp {
		Result string `json:"result"`
	}
)

@server(
	group: basic
)
service core-api {
	@doc "ping"
	@handler Ping
	get /baize/v1/ping returns (PongResp)
}