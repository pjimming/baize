syntax = "v1"

info(
	title: "basic"
	desc: "basic apis"
	author: "panjiangming"
	email: "panjm2001@126.com"
	version: "v1"
)

type (
	PongResp {
		Result string `json:"result"`
	}
)

@server(
	group: basic
)
service core-api {
	@doc "ping"
	@handler Ping
	get /baize/v1/ping returns (PongResp)
}